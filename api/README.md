# XToxica's Under the hood api

## api route:
- GET - /api/v1/ -> status
- GET - /api/v1/help -> get help on usage
- GET - /api/v1/xtoxica/<comment> -> returns the toxicity of the comment
- GET/POST = /api/v1/xtoxica/predict -> returns the toxicity of the comment